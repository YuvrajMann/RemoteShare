const express = require('express');
const fs = require('fs');
const path = require('path');
const mime = require('mime-types'); // Make sure this is installed

const router = express.Router();

module.exports = router;